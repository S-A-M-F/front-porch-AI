name: flutter_inappwebview_linux
description: "No-op stub for Linux CI builds (app uses external browser fallback)"
version: 0.1.0-beta.1

environment:
  sdk: ^3.10.8
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_inappwebview_platform_interface: ^1.0.0
