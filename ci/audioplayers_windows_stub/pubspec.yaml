name: audioplayers_windows
description: "No-op stub for Windows CI builds (newer MSVC toolchain treats <experimental/coroutine> as hard error in the plugin)"
version: 0.1.0-beta.1

environment:
  sdk: ^3.10.8
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  audioplayers_platform_interface: ^7.0.0
